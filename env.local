# Disable AWS services to prevent costs during local development
DISABLE_CLOUDWATCH=true
DISABLE_XRAY=true

# Environment settings
NODE_ENV=development
ENVIRONMENT=dev
SERVICE_NAME=organizer-management-service

# Disable tracing
ENABLE_TRING=false
